line-length = 100
target-version = "py312"
exclude = ["build", "dist", ".venv", ".git", "__pycache__"]

[lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "C4", "SIM", "PTH"]
ignore = ["B008"]

[lint.isort]
known-first-party = ["app"]
