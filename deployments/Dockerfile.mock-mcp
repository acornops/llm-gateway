FROM python:3.12.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn

COPY test/mock_mcp_server.py /app/mock_mcp_server.py

EXPOSE 8002

CMD ["uvicorn", "mock_mcp_server:app", "--host", "0.0.0.0", "--port", "8002"]
